{
    "name": "diet-app",
    "private": true,
    "version": "0.1.0",
    "description": "Diet Management System monorepo (NestJS + Next.js + React Native + PostgreSQL) with Clean Architecture",
    "scripts": {
        "build": "pnpm -r run build",
        "dev:web": "pnpm --filter @diet/web dev",
        "dev:api": "pnpm --filter @diet/api start:dev",
        "dev:mobile": "pnpm --filter @diet/mobile start",
        "lint": "pnpm -r run lint",
        "test": "pnpm -r run test"
    },
    "dependencies": {
        "@nestjs/config": "^4.0.2",
        "@nestjs/cqrs": "^11.0.3",
        "@nestjs/passport": "^11.0.5",
        "@nestjs/swagger": "^11.2.3",
        "@prisma/client": "^7.1.0",
        "bcrypt": "^6.0.0",
        "class-transformer": "^0.5.1",
        "class-validator": "^0.14.3",
        "jsonwebtoken": "^9.0.3",
        "passport": "^0.7.0",
        "passport-jwt": "^4.0.1",
        "pino": "^10.1.0",
        "pino-pretty": "^13.1.3",
        "swagger-ui-express": "^5.0.1"
    },
    "devDependencies": {
        "prisma": "^7.1.0",
        "typescript": "^5.6.3"
    }
}